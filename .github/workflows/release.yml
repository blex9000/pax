name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-appimage:
    name: Build AppImage (Linux x86_64)
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-linux-release-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-linux-release-

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-4-dev \
            libadwaita-1-dev \
            libvte-2.91-gtk4-dev \
            libgtksourceview-5-dev \
            libfuse2

      - name: Build AppImage
        run: |
          chmod +x scripts/build-appimage.sh
          scripts/build-appimage.sh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Pax-AppImage-x86_64
          path: Pax-*.AppImage

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: Pax-*.AppImage
          generate_release_notes: true

  # ── macOS build (disabled until macOS support is complete) ──
  # build-macos:
  #   name: Build macOS (arm64)
  #   runs-on: macos-latest
  #
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Install Rust
  #       uses: dtolnay/rust-toolchain@stable
  #
  #     - name: Cache Cargo
  #       uses: actions/cache@v4
  #       with:
  #         path: |
  #           ~/.cargo/registry
  #           ~/.cargo/git
  #           target
  #         key: cargo-macos-release-${{ hashFiles('**/Cargo.lock') }}
  #         restore-keys: cargo-macos-release-
  #
  #     - name: Install system dependencies
  #       run: brew install gtk4 libadwaita gtksourceview5 pkg-config
  #
  #     - name: Build
  #       run: cargo build --release --no-default-features --features sourceview
  #
  #     - name: Package
  #       run: |
  #         mkdir -p Pax.app/Contents/MacOS
  #         cp target/release/pax Pax.app/Contents/MacOS/
  #         cp -r resources Pax.app/Contents/MacOS/
  #         tar czf Pax-macos-arm64.tar.gz Pax.app
  #
  #     - name: Upload artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: Pax-macOS-arm64
  #         path: Pax-macos-arm64.tar.gz
  #
  #     - name: Add to GitHub Release
  #       if: startsWith(github.ref, 'refs/tags/')
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         files: Pax-macos-arm64.tar.gz
